\section{Nền tảng lý thuyết}
\label{sec:theory}

\subsection{Bài toán Frequent Itemset Mining}

Gọi $I = \{i_1, i_2, \ldots, i_m\}$ là tập tất cả item. Một cơ sở dữ liệu giao dịch được ký hiệu là $D = \{T_1, T_2, \ldots, T_n\}$, trong đó mỗi giao dịch $T_j \subseteq I$. Một itemset $X$ là một tập con của $I$. Ta nói giao dịch $T_j$ chứa $X$ nếu $X \subseteq T_j$.

Độ hỗ trợ tuyệt đối của $X$ trên $D$ là số giao dịch chứa $X$:
\[
    \operatorname{sup}(X) = |\{T_j \in D \mid X \subseteq T_j\}|.
\]
Độ hỗ trợ tương đối là $\operatorname{sup}_{rel}(X) = \operatorname{sup}(X) / |D|$. Với ngưỡng hỗ trợ tối thiểu $\theta$, itemset $X$ được gọi là phổ biến nếu $\operatorname{sup}_{rel}(X) \geq \theta$, hay tương đương $\operatorname{sup}(X) \geq \lceil \theta |D| \rceil$.

Bên cạnh tập phổ biến thông thường, hai biến thể quan trọng là tập đóng và tập tối đại. Một itemset phổ biến $X$ là \textit{closed itemset} nếu không tồn tại itemset phổ biến $Y$ sao cho $X \subset Y$ và $\operatorname{sup}(X) = \operatorname{sup}(Y)$. Một itemset phổ biến $X$ là \textit{maximal itemset} nếu không tồn tại itemset phổ biến $Y$ sao cho $X \subset Y$. Do đó mọi maximal itemset đều là closed itemset, nhưng không phải mọi closed itemset đều là maximal itemset. Tập maximal là biểu diễn cô đọng nhất của không gian phổ biến: biết toàn bộ maximal itemset là đủ để xác định một itemset bất kỳ có phổ biến hay không (nó phổ biến khi và chỉ khi là tập con của một maximal itemset), dù không khôi phục được chính xác support của từng tập con. Đây chính là đối tượng mà FP-Max khai thác.

Tính chất quan trọng nhất của FIM là tính phản đơn điệu của support, thường được gọi là tính chất Apriori \cite{Agrawal1993AssociationRules}. Nếu $X \subseteq Y$ thì mọi giao dịch chứa $Y$ cũng chứa $X$, nên:
\[
    \operatorname{sup}(Y) \leq \operatorname{sup}(X).
\]
Hệ quả là nếu $X$ không phổ biến thì mọi siêu tập của $X$ cũng không phổ biến. Ngược lại, nếu $Y$ phổ biến thì mọi tập con của $Y$ cũng phổ biến. Tính chất này cho phép các thuật toán FIM tỉa không gian tìm kiếm rất mạnh, và cũng là cơ sở để FP-Max tỉa theo siêu tập như trình bày bên dưới.

\subsection{FP-Growth và FP-tree}

FP-Max không khai thác trực tiếp từ cơ sở dữ liệu mà tái sử dụng cấu trúc FP-tree cùng cơ chế cơ sở mẫu điều kiện của FP-Growth. Vì vậy phần này tóm tắt cơ chế đó trước khi đi vào FP-Max.

Apriori khai thác tính chất phản đơn điệu bằng cách sinh ứng viên theo từng mức độ dài \cite{Agrawal1994FastAlgorithms}. Cách làm này dễ hiểu nhưng phải quét cơ sở dữ liệu nhiều lần và có thể sinh rất nhiều ứng viên trung gian. FP-Growth thay đổi hướng tiếp cận: thuật toán chỉ quét dữ liệu hai lần để xây dựng một cây nén gọi là FP-tree, sau đó khai thác mẫu bằng các cơ sở mẫu điều kiện mà không cần sinh ứng viên toàn cục \cite{Han2000FPGrowth}.

Trực giác của FP-Growth là nhiều giao dịch có tiền tố giống nhau sau khi các item được sắp theo support giảm dần. Khi chèn các giao dịch đã sắp xếp vào cùng một cây, các tiền tố chung được gộp lại và đếm bằng biến \textit{count} trên node. Header table lưu danh sách các node ứng với cùng một item, nhờ đó thuật toán có thể tìm nhanh các đường đi kết thúc tại item đang xét. Với mỗi item, thuật toán thu thập conditional pattern base, dựng conditional FP-tree, rồi đệ quy khai thác các mẫu có hậu tố là item đó.

Một node trong FP-tree gồm bốn thành phần: item đang đại diện, biến đếm số giao dịch đi qua node, con trỏ tới node cha, và tập các node con. Root không chứa item thật. Header table ánh xạ mỗi item tới danh sách các node mang item đó trong cây.

Ví dụ với cơ sở dữ liệu toy ở Mục \ref{sec:example} và $\theta = 0.6$, các item phổ biến là $2,3,5,1$ với support lần lượt $4,4,4,3$. Sau khi loại item không phổ biến và sắp item theo support giảm dần, FP-tree có dạng khái niệm trong Hình \ref{fig:fptree-toy}.

\begin{figure}[H]
    \centering
    \begin{tikzpicture}[
        level distance=1.25cm,
        level 1/.style={sibling distance=4.2cm},
        level 2/.style={sibling distance=2.3cm},
        level 3/.style={sibling distance=1.8cm},
        every node/.style={draw, rounded corners, align=center, minimum width=1.7cm}
    ]
    \node {root}
        child { node {$3:1$}
            child { node {$1:1$} }
        }
        child { node {$2:4$}
            child { node {$3:3$}
                child { node {$5:3$}
                    child { node {$1:2$} }
                }
            }
            child { node {$5:1$} }
        };
    \end{tikzpicture}
    \caption{FP-tree minh hoạ cho CSDL toy ở Mục \ref{sec:example} với minsup $=0.6$.}
    \label{fig:fptree-toy}
\end{figure}

Cây trên nén năm giao dịch thành sáu node item. Hai giao dịch $[2,3,5,1]$ trùng tiền tố hoàn toàn nên được biểu diễn bằng một đường đi có count tăng lên. Đây là nguồn tiết kiệm bộ nhớ chính của FP-tree, và FP-Max được hưởng lợi trực tiếp từ tính chất nén này. FP-Growth khai thác từng item: thu thập conditional pattern base, tỉa các item không đủ support, dựng conditional FP-tree và đệ quy; khi cây điều kiện chỉ còn một đường đi thì sinh trực tiếp các tổ hợp con thay vì dựng thêm cây.

\subsection{Ý tưởng và thuật toán FP-Max}

FP-Max khai thác tập phổ biến tối đại thay vì toàn bộ tập phổ biến \cite{Grahne2003FPMax}. Điểm khác biệt cốt lõi so với FP-Growth là: thay vì xuất ra mọi itemset phổ biến gặp được, FP-Max chỉ giữ lại các itemset không có siêu tập phổ biến nào, và dùng chính các tối đại đã tìm được để \emph{tỉa} những nhánh chắc chắn không sinh thêm tối đại mới. Nhờ đó FP-Max tránh được việc liệt kê số lượng tập phổ biến có thể lớn theo cấp số mũ trên dữ liệu dày.

FP-Max sử dụng hai cấu trúc đặc trưng:

\begin{itemize}
    \item \textbf{Danh sách MFI} (maximal frequent itemset list): tập các tối đại đã tìm được. Khi có một candidate mới, nếu nó là tập con của một MFI sẵn có thì bỏ qua; ngược lại, mọi MFI cũ là tập con nghiêm ngặt của candidate bị loại, rồi thêm candidate vào. Bất biến của danh sách là không có hai phần tử nào lồng nhau.
    \item \textbf{Tỉa theo siêu tập} (superset pruning): tại một head hiện tại, gọi tail là tập các item còn đủ support trong conditional pattern base. Nếu $head \cup tail$ đã là tập con của một MFI có sẵn thì toàn bộ nhánh đệ quy bắt nguồn từ head không thể sinh ra tối đại mới (mọi tối đại của nhánh đều nằm trong $head \cup tail$, do đó nằm trong MFI đó), nên nhánh bị cắt mà không cần dựng conditional FP-tree.
\end{itemize}

Để phép tỉa phát huy tác dụng sớm, FP-Max xét các item trong header theo thứ tự support tăng dần. Item hiếm được xử lý trước thường sinh ra các tối đại "sâu và dài", giúp danh sách MFI lớn nhanh và các item phổ biến hơn (xử lý sau) dễ rơi vào trường hợp bị tỉa. Khi cây điều kiện co lại thành một đường đi, toàn bộ đường đi hợp với head tạo thành đúng một candidate tối đại, không cần liệt kê $2^k-1$ tập con như FP-Growth.

\begin{algorithm}[H]
\caption{FP-Max}
\begin{algorithmic}[1]
\Require FP-tree, head hiện tại, danh sách MFI
\Ensure Danh sách maximal frequent itemsets
\If{FP-tree chỉ có một đường đi} \Comment{trường hợp tốt nhất: một candidate duy nhất}
    \State $candidate \gets head \cup$ các item trên đường đi
    \State Chèn $candidate$ vào MFI nếu nó không là tập con của MFI hiện có
    \State Xoá các MFI cũ là tập con nghiêm ngặt của $candidate$
    \State \Return
\EndIf
\State Sắp các item trong header theo support tăng dần \Comment{tỉa sớm}
\For{mỗi item $a$}
    \State $head \gets head \cup \{a\}$
    \State Tạo conditional pattern base và $tail$ = các item còn phổ biến của $a$
    \If{$head \cup tail$ không là tập con của MFI nào} \Comment{superset pruning}
        \State Dựng conditional FP-tree và đệ quy với head mới
    \EndIf \Comment{ngược lại: cắt cả nhánh, không dựng cây điều kiện}
    \State Bỏ $a$ khỏi head
\EndFor
\end{algorithmic}
\end{algorithm}

Điểm mấu chốt là FP-Max không bao giờ vật chất hoá toàn bộ tập phổ biến. Mỗi tối đại được phát hiện hoặc ở nhánh single-path, hoặc khi một nhánh đệ quy kết thúc; còn các nhánh không thể đóng góp tối đại mới bị loại nhờ phép kiểm tra siêu tập. Support cuối cùng của mỗi MFI được tính lại bằng một lần đối chiếu với cơ sở dữ liệu, vì trong quá trình tỉa thuật toán chỉ quan tâm tới tính phổ biến chứ chưa cần giá trị support chính xác.

\subsection{Phân tích độ phức tạp}

\paragraph{Độ phức tạp thời gian.}
Chi phí gồm hai giai đoạn: xây FP-tree và khai thác đệ quy. Giai đoạn xây cây cần hai lần quét CSDL --- một để đếm support, một để chèn giao dịch đã sắp --- với chi phí $\mathcal{O}(L\log\bar{\ell})$ do mỗi giao dịch phải sắp lại theo support ($L$ là tổng số item trên toàn bộ giao dịch, $\bar{\ell}$ là độ dài giao dịch trung bình sau tỉa). Giai đoạn khai thác phụ thuộc số tập tối đại $M$ và mức độ phép tỉa cắt được nhánh.

\begin{itemize}
  \item \textbf{Tốt nhất (single-path):} cây co thành một đường, sinh trực tiếp đúng một tối đại mà không cần dựng conditional tree hay liệt kê tổ hợp con. Khai thác $\mathcal{O}(\bar{\ell})$, tổng $\mathcal{O}(L\log\bar{\ell})$. Đây là điểm vượt trội rõ rệt so với FP-Growth: trên cùng đường đi dài $k$, FP-Growth xuất $2^k-1$ tập phổ biến còn FP-Max chỉ xuất một.
  \item \textbf{Trung bình:} phép tỉa theo siêu tập cắt phần lớn các nhánh không sinh tối đại mới, nên số conditional tree thực sự dựng nhỏ hơn nhiều số tập phổ biến. Chi phí xấp xỉ $\mathcal{O}(L\log\bar{\ell} + M\cdot c)$, trong đó $c$ gộp chi phí trung bình dựng/duyệt một cây điều kiện và chi phí kiểm tra/cập nhật danh sách MFI (mỗi thao tác $\mathcal{O}(|MFI|\cdot\bar{\ell})$ với danh sách MFI tuyến tính).
  \item \textbf{Xấu nhất:} mọi tập phổ biến đều là tối đại (một phản xích, không tập phổ biến nào chứa tập phổ biến khác), phép tỉa không cắt được gì và $M$ có thể đạt $\binom{m}{m/2}\approx 2^m/\sqrt{m}$. Chi phí khai thác bùng nổ mũ $\mathcal{O}(2^m)$, chi phối toàn bộ thời gian chạy.
\end{itemize}

\begin{table}[H]
  \centering
  \caption{Độ phức tạp thời gian của FP-Max theo từng trường hợp.}
  \label{tab:time-complexity}
  \begin{tabular}{lcc}
    \toprule
    Trường hợp & Xây cây & Khai thác \\
    \midrule
    Tốt nhất    & $\mathcal{O}(L\log\bar{\ell})$ & $\mathcal{O}(\bar{\ell})$ \\
    Trung bình  & $\mathcal{O}(L\log\bar{\ell})$ & $\mathcal{O}(M\cdot c)$ \\
    Xấu nhất    & $\mathcal{O}(L\log\bar{\ell})$ & $\mathcal{O}(2^m)$ \\
    \bottomrule
  \end{tabular}
\end{table}

Kết quả này nhất quán với thực nghiệm: thời gian chạy tăng theo số tập tối đại đầu ra (Mục~\ref{sec:evaluation}), và bước nhảy mạnh trong thí nghiệm độ dài giao dịch (Bảng~\ref{tab:txnlen}) xuất hiện đúng khi cấu trúc dữ liệu đẩy $M$ tăng nhanh, phù hợp với cận mũ ở trường hợp xấu.

\paragraph{Độ phức tạp không gian.}
Bộ nhớ FP-Max gồm FP-tree cùng header table, các conditional FP-tree sinh trong đệ quy, danh sách MFI, và tập kết quả. Gọi $N$ là số node thực tế của FP-tree, $M$ số tập tối đại.

\begin{itemize}
  \item \textbf{Tốt nhất (single-path):} mọi giao dịch chung tiền tố, cây co thành một đường dài $\mathcal{O}(\bar{\ell})$, không sinh conditional tree và danh sách MFI chỉ có một phần tử. Bộ nhớ $\mathcal{O}(\bar{\ell})$, độc lập với $n$.
  \item \textbf{Trung bình:} nén tiền tố tốt nên $N \ll L$; các conditional tree dọc một nhánh được giải phóng khi nhánh kết thúc, độ sâu đệ quy $\mathcal{O}(\bar{\ell})$; danh sách MFI chiếm $\mathcal{O}(M\cdot\bar{\ell})$. Bộ nhớ làm việc xấp xỉ $\mathcal{O}(N + M\bar{\ell})$, khớp với Bảng~\ref{tab:peak-rss}: peak RSS chỉ vượt sàn vài chục MB.
  \item \textbf{Xấu nhất:} không có tiền tố chung nên $N = \mathcal{O}(L)$; nếu mọi tập phổ biến đều tối đại thì $M = \mathcal{O}(2^m/\sqrt{m})$, danh sách MFI và output chiếm ưu thế. Tổng $\mathcal{O}(L + M\bar{\ell})$.
\end{itemize}

\begin{table}[H]
  \centering
  \caption{Độ phức tạp không gian của FP-Max theo từng trường hợp.}
  \label{tab:space-complexity}
  \begin{tabular}{lccc}
    \toprule
    Trường hợp & Cấu trúc & MFI + output & Tổng \\
    \midrule
    Tốt nhất    & $\mathcal{O}(\bar{\ell})$       & $\mathcal{O}(\bar{\ell})$    & $\mathcal{O}(\bar{\ell})$ \\
    Trung bình  & $\mathcal{O}(N),\ N \ll L$      & $\mathcal{O}(M\bar{\ell})$   & $\mathcal{O}(N + M\bar{\ell})$ \\
    Xấu nhất    & $\mathcal{O}(L)$                & $\mathcal{O}(M\bar{\ell})$   & $\mathcal{O}(L + M\bar{\ell})$ \\
    \bottomrule
  \end{tabular}
\end{table}

Cận trên ở đây là \emph{đỉnh thường trú} (\texttt{Sys.maxrss}), khác với tổng \texttt{alloc\_bytes} (Bảng~\ref{tab:perf-alloc}) vốn cộng dồn cả các \texttt{Vector} tạm bị GC thu hồi --- nên tổng cấp phát thường lớn hơn đỉnh thường trú nhiều lần.

\paragraph{Các yếu tố ảnh hưởng chính.}
Hiệu năng FP-Max phụ thuộc vào: (i) số tập tối đại $M$, vốn tăng nhanh khi minsup giảm hoặc dữ liệu dày; (ii) độ dài giao dịch trung bình $\bar{\ell}$, vì giao dịch dài làm cây sâu hơn và tăng số item đồng xuất hiện; (iii) mức độ chia sẻ tiền tố, quyết định độ nén của FP-tree. So với FP-Growth, FP-Max ít nhạy với số tập \emph{phổ biến} hơn vì phép tỉa và biểu diễn tối đại cô đọng, nhưng vẫn nhạy với $M$ trong trường hợp xấu.

\subsection{Vị trí lịch sử}

Apriori đặt nền móng cho FIM bằng tính chất phản đơn điệu, nhưng bị giới hạn bởi số lần quét dữ liệu và số lượng ứng viên lớn \cite{Agrawal1994FastAlgorithms}. FP-Growth giải quyết hạn chế này bằng FP-tree và khai thác mẫu không sinh ứng viên \cite{Han2000FPGrowth}, song trên dữ liệu dày nó vẫn phải xuất ra số tập phổ biến lớn theo cấp số mũ. FP-Max (trong họ FPgrowth*/FPmax* của Grahne và Zhu) \cite{Grahne2003FPMax} khắc phục đúng điểm này: tái dùng FP-tree nhưng chỉ khai thác biểu diễn tối đại, dùng danh sách MFI và phép tỉa theo siêu tập để cắt không gian tìm kiếm. Đổi lại, FP-Max chỉ giữ vùng biên của không gian phổ biến nên không khôi phục được support của mọi tập con nếu không quét lại dữ liệu --- đây là lý do phần ứng dụng sinh luật kết hợp (Mục~\ref{sec:application}) vẫn cần cài đặt khai thác đầy đủ tập phổ biến. Song song với hướng dạng cây, các thuật toán dạng dọc như Eclat dùng tidset/diffset \cite{Zaki2000Eclat}, và các thư viện thực nghiệm như SPMF cung cấp nhiều cài đặt tham chiếu \cite{FournierViger2014SPMF}, trong đó có FPMax dùng làm chuẩn đối chiếu ở Mục~\ref{sec:evaluation}.
